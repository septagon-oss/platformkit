{
  "name": "platformkit-e2e",
  "private": true,
  "description": "Gate 10: one Playwright spec, against a real instance of the reference app.",
  "scripts": {
    "test": "playwright test",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.55.0"
  }
}
